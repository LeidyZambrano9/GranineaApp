<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">

        <!-- Home: ícono de casa/día -->
        <item
            android:id="@+id/nav_home"
            android:icon="@android:drawable/ic_menu_day"
            android:title="Home" />

        <!-- Pedidos: ícono de agenda/gestión -->
        <item
            android:id="@+id/nav_pedidos"
            android:icon="@android:drawable/ic_menu_agenda"
            android:title="Pedidos" />

        <!-- Productos: ícono de galería/catálogo -->
        <item
            android:id="@+id/nav_productos"
            android:icon="@android:drawable/ic_menu_gallery"
            android:title="Productos" />

        <!-- Usuarios: ícono de contactos/personas -->
        <item
            android:id="@+id/nav_usuarios"
            android:icon="@android:drawable/menu_frame"
            android:title="Usuarios" />

        <!-- Perfil: ícono de configuración/preferencias -->
        <item
            android:id="@+id/nav_perfil"
            android:icon="@android:drawable/ic_menu_preferences"
            android:title="Perfil" />

    </group>

</menu>